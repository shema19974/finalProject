<div style="margin-left:20px">
    <%= form_for ([@post, @post.comments.build]) do |f| %>
        <%= f.text_area :content, placeholder: 'Enter a comment...', required:'true' %><br>
        <%= f.submit "Add comment", style:"margin-bottom:20px;"  %>
    <%end%>
<div>